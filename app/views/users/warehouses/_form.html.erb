<%= form_with(model: users_warehouse) do |form| %>
  <% if users_warehouse.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(users_warehouse.errors.count, "error") %> prohibited this users_warehouse from being saved:</h2>

      <ul>
        <% users_warehouse.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :address, style: "display: block" %>
    <%= form.text_field :address %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
