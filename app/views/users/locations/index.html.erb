<!DOCTYPE html>
<html>

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Locations</title>
  <%= stylesheet_link_tag "application", "data-turbo-track" => "reload" %> <!-- Link to your application.css -->
</head>

<body>

  <div class="container">
    <h1 class="mt-5 mb-4" style="text-align: center;">Locations</h1>

    <table class="table table-striped">
      <thead>
        <tr>
          <th>Name</th>
          <th>Latitude</th>
          <th>Longitude</th>
          <th colspan="2">Actions</th>
        </tr>
      </thead>
      <tbody>
        <% @locations.each do |location| %>
          <tr>
            <td><%= location.name %></td>
            <td><%= location.lat %></td>
            <td><%= location.lng %></td>
            <td>
              <%= link_to "Show", users_location_path(location), class: "btn btn-info btn-sm me-2" %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <%= link_to "Back to User Dashboard", users_page_path, class: "btn btn-secondary mt-3" %>
  </div>

</body>

</html>
